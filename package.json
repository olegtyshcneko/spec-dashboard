{
  "name": "spec-dashboard",
  "version": "0.9.0",
  "private": true,
  "description": "Git-native MDX spec and planning dashboard compiler",
  "license": "MIT",
  "type": "module",
  "bin": {
    "specdash": "./packages/cli/dist/bin.mjs",
    "specdash-mcp": "./packages/mcp/dist/bin.mjs"
  },
  "files": [
    ".agents/**",
    "assets/**",
    "docs/**",
    "packages/*/dist/**",
    "packages/*/src/**",
    "packages/*/test/**",
    "packages/*/package.json",
    "packages/*/tsconfig.json",
    "packages/renderer/astro.config.mjs",
    "plugins/**",
    "README.md",
    "LICENSE",
    "specdash.config.yaml",
    "tsconfig.base.json"
  ],
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:packages": "npm run build -w @spec-dashboard/core && npm run build -w @spec-dashboard/cli && npm run build -w @spec-dashboard/mcp",
    "build": "npm run build:packages && node packages/cli/dist/index.js build --root .",
    "dev": "npm run build:packages && node packages/cli/dist/index.js dev --root .",
    "validate": "npm run build:packages && node packages/cli/dist/index.js validate --root .",
    "reconcile": "npm run build:packages && node packages/cli/dist/index.js reconcile --root .",
    "test": "npm run build:packages && npm run test -w @spec-dashboard/core && npm run test -w @spec-dashboard/mcp",
    "mcp": "npm run build:packages && node packages/mcp/dist/index.js --root .",
    "prepare": "npm run build:packages"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "dependencies": {
    "@astrojs/mdx": "^7.0.2",
    "@modelcontextprotocol/sdk": "1.29.0",
    "astro": "^7.0.7",
    "fast-glob": "^3.3.3",
    "gray-matter": "^4.0.3",
    "yaml": "^2.8.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
